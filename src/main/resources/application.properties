spring.application.name=blotto-game

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=blotto_db
spring.data.mongodb.username=admin
spring.data.mongodb.password=password
#spring.data.mongodb.authentication-database=admin  # Authenticate against admin

#server.port=${PORT:8080}  # Fly.io provides the PORT environment variable
#server.address=0.0.0.0    # Bind to all network interfaces
